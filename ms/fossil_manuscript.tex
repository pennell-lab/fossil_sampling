% fossil project manuscript


% ---------------------------------------------------------------------------- %
  \documentclass[12pt,twoside,a4paper,pdftex]{scrbook}

% ---------------------------------------------------------------------------- %
  % Pacotes 
% ,top=2.54cm,bottom=2.0cm,left=2.0cm,right=2.54cm % old definition of the margins....
\usepackage[a4paper,top=2.54cm,bottom=2.0cm,left=2.0cm,right=2.54cm]{geometry} % define que a página usada é A4 e o tamanho das margens
\usepackage[utf8x]{inputenc} % para escrever o documento diretamente com os acentos, e não usando macros (“á” ao invés de “\´a”)
\usepackage[T1]{fontenc} % muda a codificação da fonte, tornando possível copiar textos com acentos.
\usepackage{textcomp} % para poder usar simbolos no arquivo
\usepackage{setspace} %para poder definir o espaçamento entre linhas. Pode ser: \singlespacing, espaçamento simples; \onehalfspacing, espaçamento de 1,5; \doublespacing, espaçamento duplo
\usepackage{graphicx} %para poder incluir imagens no meio do texto.
\usepackage{color} % mudar a cor do texto.
\usepackage{nameref} %quando uma sessão for citada ela aparecerá com o nome e não o número
\usepackage{hyperref} %para fazer links dentro do .pdf (na referência por exemplo)
\usepackage{layout} %para saber como a página está organizada, usar \layout depois de começar o documento
\usepackage{rotating} % Tables can also be put on their side within a document using the rotating package and the sidewaystable environments in place of the table environment.
\usepackage{amsthm}
\usepackage{amsmath}




\usepackage{natbib} % comando para poder citar authordate. permite o uso de citep, citet, etc


\definecolor{graytab}{gray}{0.85}


\begin{document}
\section{Abstract}
blah blah blah


\end{document}